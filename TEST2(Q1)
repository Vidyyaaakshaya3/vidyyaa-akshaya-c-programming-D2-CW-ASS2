#include <stdio.h>
int main()
{
	printf("enter the row size:");
	int row_size,out,in;
	scanf("%d",&row_size);
	
	for(out=1;out<=row_size;out++)
	{
		for(in=1;in<=out;in++)
		{
			printf("%d",out);
		}
		printf("\n");
}
}
